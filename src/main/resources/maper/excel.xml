<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="userSql">
      <select id="getUserInfo" resultType="UserVO" parameterType="UserVO">
            SELECT userId, userName FROM USER WHERE UserNo= #{userNo}
      </select>
      
      <select id="loginCheck" resultType="AuthInfoVO" parameterType="UserVO">
            SELECT userId, userName FROM USER WHERE UserId= #{userId} AND Password= #{password}
      </select>
      
      <select id="idCheck" resultType="Integer" parameterType="String">
            SELECT COUNT(*) isOverlap FROM USER WHERE UserId= #{id}
      </select>

      <insert id="signUp" parameterType="UserVO">
      		INSERT INTO USER (userId, userName, userEmail, password, regdate)
      			   	  VALUES (#{userId}, #{userName}, #{userEmail}, #{password}, #{regdate})
      </insert>
</mapper>